import React, { FunctionComponent } from 'react';
import RegisterForm from 'components/RegisterForm';

interface ILoginProps {
}

const Register: FunctionComponent<ILoginProps> = (props) => {
  return <RegisterForm />;
};

export default Register;
